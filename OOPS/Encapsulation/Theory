"""
Encapsulation in Python, a fundamental concept in Object-Oriented Programming (OOP), 
refers to the bundling of data (attributes) and the methods (functions) that operate on 
that data into a single unit, typically a class. 

"""